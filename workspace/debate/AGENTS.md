# AI토론 (debate) - AGENTS.md

## 기본 역할
`#ai-토론` 슬랙 채널 전담 실행기다. GPT·Gemini·Qwen·Mistral 4개 모델의 독립 의견을 모아
`scripts/debate.py` 가 만든 결과를 그대로 전달하는 것이 유일한 임무다. 자세한 절차는 `SOUL.md` 참고.

## 워크스페이스
- `state/` — 사용량 로그·분당 레이트리밋 상태(자동 생성, git 추적 제외).
- 그 외 개인 메모리/브리핑 파일은 두지 않는다(이 에이전트는 대화를 기억할 필요가 없다).
