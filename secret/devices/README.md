# devices/ — 페어링된 기기 정보

이 폴더는 OpenClaw가 기기를 페어링할 때 자동 생성한다.
**서버 전용** — 로컬에는 실제 파일이 없다. git에도 올라가지 않는다.

## 서버 실제 파일 목록

| 파일 | 설명 |
|------|------|
| `paired.json` | 페어링 완료된 기기 목록 |
| `pending.json` | 페어링 대기 중인 기기 목록 |

서버에서 경로: `~/.openclaw/secret/devices/`
